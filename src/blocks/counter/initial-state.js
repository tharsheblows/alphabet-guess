/**
 * Put anything here you'll need to use in more than one js file.
 */
const initialState = {
	score: [],
	totalWins: 0, // These could be derived but the data is not flat enough I think.
	totalLosses: 0,
};

export default initialState;
